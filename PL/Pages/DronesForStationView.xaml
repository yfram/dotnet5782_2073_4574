<UserControl x:Class="PL.Pages.DronesForStationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL.Pages" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignWidth="800" Height="218">
    <Grid>

        <Button x:Name="ExitButton" Click="Exit" Margin="0,10,10,0" Background="Red" BorderThickness="0" Padding="0" Height="32" VerticalAlignment="Top" HorizontalAlignment="Right" Width="32">
            <materialDesign:PackIcon Kind="FullscreenExit"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        </Button>
        <ListView x:Name="ListOfDrones" Background="{x:Null}" Foreground="Black" Margin="0,40,50,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
